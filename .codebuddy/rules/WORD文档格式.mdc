---
description: Word文档排版标准 - 两套标题体系、字体段落、页面设置、表格样式、代码实现
alwaysApply: true
enabled: true
updatedAt: 2026-06-17T22:30:00.000Z
provider: 
---

# Word 文档排版标准（强制）

所有生成的 Word 文档必须严格遵循以下排版标准。详细指南见 `03_模板工具/报表格式理解指南.md`。

## ⚠️ 两套标题体系（按文档类型选用）

| 层级 | 体系A-月度财务情况说明 | 体系B-经营财务月报 |
|------|----------------------|-------------------|
| 一级标题 | **方正小标宋简体 22pt** | **黑体 16pt** |
| 二级标题 | **黑体 16pt** | **楷体_GB2312 16pt** |
| 三级标题 | 仿宋_GB2312 16pt | 仿宋_GB2312 16pt |
| 适用场景 | 正式月度说明、述职报告 | 经营月报、PPT配套简报 |

> 默认使用体系A，仅当明确指定"月报模板"时才使用体系B。

## 字体与段落（通用）

- **正文**：仿宋_GB2312，三号（16pt），不加粗，黑色
- **行间距**：固定值30磅（`line_spacing_rule=2, line_spacing=Pt(30)`）
- **段前段后**：0行（`space_before=Pt(0), space_after=Pt(0)`）
- **首行缩进**：2字符（约 `Pt(32)`，16pt字号×2）
- 所有文字统一**黑色** `RGBColor(0,0,0)`

## 页面设置

- 左边距：2.8cm | 右边距：2.6cm
- 上边距：3.7cm | 下边距：3.5cm
- 纸张：A4（21cm × 29.7cm）

## 表格样式

- 表格内字体：**宋体 10pt**（非仿宋！）
- 表头：深蓝底 `#1F4E79`，白字 `#FFFFFF`，**黑体加粗 10pt**
- 数据行：无底色，行高最小值
- 边框：`Table Grid` 样式

## python-docx 关键实现要点

1. **中文字体必须同时设置 `w:eastAsia`**：`run._element.rPr.rFonts.set(qn('w:eastAsia'), '字体名')`
2. **行距用固定值而非倍数**：`pf.line_spacing_rule = 2`（WD_LINE_SPACING.EXACTLY）
3. **表头底色用 XML 直接设置**：`shading = parse_xml(f'<w:shd {nsdecls("w")} w:fill="1F4E79"/>')`
4. **显式设置 bold=False**：Word 默认样式可能自带加粗
5. **段落格式在设置文本之后调整**：先 `add_paragraph(text)` 再调 `paragraph_format`

## 参考文件

- 完整实现：`03_模板工具/生成Word简报模板.py` — `set_heading_style()` / `set_body_style()`
- 占位符模板：`03_模板工具/生成文字模板.py` — `add_para()` / `add_table()`
- 综合指南：`03_模板工具/报表格式理解指南.md` — 含代码片段、常见错误、工作流