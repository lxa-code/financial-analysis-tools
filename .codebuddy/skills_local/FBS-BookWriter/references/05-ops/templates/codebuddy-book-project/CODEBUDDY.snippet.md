# 本书项目 · CodeBuddy / WorkBuddy 记忆片段

> 由 FBS-BookWriter 模板生成或合并；可随官方 [记忆文档](https://www.codebuddy.cn/docs/cli/memory) 调整。

## 本书固定上下文

- **技能包根目录**：`__FBS_SKILL_ROOT__`（须为含 `SKILL.md` 与 `references/` 的目录）
- **体裁 / 读者**：（在此填写，避免每轮重复说明）
- **成稿目录**：例如 `./manuscript/` 或 `./src/`（与构建脚本一致）

## 省 token 约定（重要）

- **不要**让模型一次性通读整个 `references/`。
- 需要工作流与检索门禁时，只导入单文件，例如：

```markdown
工作流与强制检索见 @__FBS_SKILL_ROOT__/references/01-core/section-3-workflow.md 中 §3.0.5
门禁配置见 @__FBS_SKILL_ROOT__/references/05-ops/search-policy.json
```

- 需要质量检查时再 `@__FBS_SKILL_ROOT__/references/02-quality/quality-check.md`（按需）。

## FBS-BookWriter 触发词（精选）

与 `SKILL.md` Frontmatter 一致：写书、写长篇、写手册、写白皮书、写行业指南、协同写书、定大纲、写章节、排版构建、导出、去AI味、质量自检、图文书（Mermaid/封面）。

## CodeBuddy Added Memories（可追加个人偏好）

### 语言与排版

- 正文：简体中文；术语表是否中英对照：（填是/否）
