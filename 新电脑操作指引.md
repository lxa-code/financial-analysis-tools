# 新电脑操作指引

本指引用于在任意新电脑上快速恢复 CodeBuddy 的全部能力（记忆、插件、规则、自动化、IMA 知识库等）。所有数据已通过百度网盘同步，只需在新电脑上执行以下几步即可。

---

## 前置条件

- 百度网盘已在新电脑上同步完成，`CODE` 文件夹可用
- Windows 系统（macOS/Linux 类似，路径略有差异）

---

## 第 1 步：安装 Node.js

CodeBuddy Code 依赖 Node.js 运行。

1. 下载 Node.js：https://nodejs.org/（推荐 LTS 版本，如 20.x）
2. 安装后验证：
   ```bash
   node --version
   npm --version
   ```

---

## 第 2 步：安装 CodeBuddy Code

```bash
npm install -g @tencent-ai/codebuddy-code
```

安装后验证：
```bash
codebuddy --version
```

---

## 第 3 步：安装 Python（如需 IMA 知识库功能）

如果不需要 IMA 知识库，可跳过此步。

1. 下载 Python 3.11+：https://www.python.org/downloads/
2. 安装时勾选 "Add Python to PATH"
3. 安装后验证：
   ```bash
   python --version
   ```

---

## 第 4 步：安装 IMA MCP 依赖（如需 IMA 知识库功能）

```bash
pip install ima-mcp
```

---

## 第 5 步：配置 IMA API 凭证（如需 IMA 知识库功能）

在 PowerShell 中执行：

```powershell
# 创建配置目录
mkdir -p "$env:USERPROFILE\.config\ima"

# 写入 Client ID
"b3b03abbc9be53e598433b3ea703c5dd" | Out-File -FilePath "$env:USERPROFILE\.config\ima\client_id" -Encoding utf8

# 写入 API Key
"T4AQyTC6gitaPD+gGnmdROL6F5s5/echcxz4wp/a5+HRB9wG9NSUgq6AVL+Nl8pyTmig4k0sSQ==" | Out-File -FilePath "$env:USERPROFILE\.config\ima\api_key" -Encoding utf8
```

验证凭证是否有效：
```bash
python -c "from ima_mcp.client import IMAClient; c = IMAClient(); print(c.check_credentials())"
```

如果输出包含 `'valid': True`，说明配置成功。

---

## 第 6 步：打开工作区

1. 启动 CodeBuddy
2. 打开百度网盘同步文件夹中的工作区目录（路径类似 `D:\BaiduSyncdisk\CODE\城发投资工作专区`，具体取决于你新电脑上百度网盘的同步位置）

CodeBuddy 会自动加载：
- 所有记忆和知识库（`.codebuddy/memery/`）
- 已启用的插件和技能（`.codebuddy/settings/`、`.codebuddy/plugins_marketplace/`）
- 项目规则（`.codebuddy/rules/`）
- MCP 配置（`.mcp.json`，含 IMA 知识库连接）
- 自动化任务（`.codebuddy/automations/`）
- 对话历史（`.codebuddy/history/`）

---

## 验证清单

打开工作区后，确认以下功能正常：

- [ ] 问一个与城发投相关的问题，AI 能基于记忆回答（说明记忆加载成功）
- [ ] 搜索 IMA 知识库（说明 MCP 配置生效）
- [ ] 使用 Excel 或 Word 技能（说明插件加载成功）

---

## 常见问题

**Q：打开工作区后 AI 不记得城发投的信息？**

检查 `.codebuddy/memery/` 目录下是否有 `f76a162f-6312-4b3d-8ab4-74216525198f_memery.md` 和 `user-memery-state.json` 两个文件。如果没有，可能是百度网盘尚未同步完成，等待同步完毕再试。

**Q：IMA 知识库无法连接？**

1. 确认已执行第 3-5 步
2. 检查 `~/.config/ima/` 下是否有 `client_id` 和 `api_key` 文件
3. 如果 API Key 过期，访问 https://ima.qq.com/agent-interface 重新获取

**Q：插件/技能加载失败？**

等待 CodeBuddy 首次打开时自动下载插件依赖（可能需要几分钟），或者手动执行 `codebuddy plugin install`。

---

## 文件结构速查

```
BaiduSyncdisk/CODE/
├── 城发投资工作专区/          # 工作区根目录（CodeBuddy 打开此目录）
│   ├── .codebuddy/            # CodeBuddy 项目配置（自动同步）
│   │   ├── memery/            # 记忆和知识库
│   │   ├── settings/          # 插件配置
│   │   ├── rules/             # 项目规则
│   │   ├── plugins_marketplace/ # 插件市场
│   │   ├── automations/       # 自动化任务
│   │   └── history/           # 对话历史
│   ├── .mcp.json              # MCP 服务器配置（含 IMA）
│   ├── 01_公司概况/
│   ├── 02_财务管理/
│   └── ...
└── skills/                    # 技能文件
```

```
C:\Users\<用户名>\.config\ima\  # IMA 凭证（需手动配置）
├── client_id
└── api_key
```

---

*最后更新：2026年6月3日*
